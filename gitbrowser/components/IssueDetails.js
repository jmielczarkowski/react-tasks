/**
 * Browse issue details and allow to put comment.
 */

import React from 'react';
import { View } from 'react-native';

export const IssueDetails = ({ navigation }) => {
  return (
    <View
    />
  );
};