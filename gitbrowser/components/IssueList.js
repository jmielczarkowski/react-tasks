/**
 * Listing on infinite scroll list issues from repository.
 */

import React from 'react';
import { View } from 'react-native';

export const IssueList = ({ navigation }) => {
  return (
    <View
    />
  );
};